import { ReturnResponse, SingleResponse } from "@/types/common.type";
import satellite from "../satellite";
import { IBlog, ICar } from "@/types/index";

export const getBlogs = async () => {};

export const getBlogDetail = async (id: string) => {};
